-- H2 profile uses Hibernate ddl-auto=update for schema creation.
-- This file intentionally contains no DDL so MySQL schema.sql is skipped.
CREATE TABLE IF NOT EXISTS h2_init_marker (
	id INT PRIMARY KEY
);
